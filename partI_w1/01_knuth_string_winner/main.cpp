#include <iostream>
#include <random>
#include <iomanip>
#include <string>

using namespace std;

int main(int argc, char** args) {
    //first
    int i = 0;
    while (args[i] != NULL)
    {
        string winner = args[i];
        if (bernoulli_distribution)

        i++;
    }

    //second
    

    return 0;
}
